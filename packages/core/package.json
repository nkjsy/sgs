{
  "name": "@sgs/core",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/test/**/*.test.js",
    "skill:status": "npm run build && node dist/skill-status.js",
    "general:status": "npm run build && node dist/general-status.js"
  }
}
