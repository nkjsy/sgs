{
  "name": "@sgs/single",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "start:interactive": "npm run -w @sgs/core build && npm run build && node dist/interactive.js",
    "stats": "npm run -w @sgs/core build && npm run build && node dist/batch-stats.js",
    "test": "node --test"
  },
  "dependencies": {
    "@sgs/core": "0.1.0"
  }
}
